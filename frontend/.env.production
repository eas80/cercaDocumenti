# Used by `npm run build` (Vite's "production" mode), e.g. on Render.
# Local dev (`npm run dev`) is unaffected — it uses the Vite proxy instead.
VITE_API_BASE_URL=https://cercadocumenti-be.onrender.com
